name: Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        nvim-version: ["stable", "nightly"]

    steps:
      - uses: actions/checkout@v4

      - name: Install Neovim
        run: |
          if [ "${{ matrix.nvim-version }}" = "nightly" ]; then
            URL="https://github.com/neovim/neovim/releases/download/nightly/nvim-linux-x86_64.tar.gz"
          else
            URL="https://github.com/neovim/neovim/releases/latest/download/nvim-linux-x86_64.tar.gz"
          fi
          curl -sL -o nvim.tar.gz "$URL"
          sudo rm -rf /opt/nvim
          sudo tar -C /opt -xzf nvim.tar.gz
          sudo ln -sf /opt/nvim-linux-x86_64/bin/nvim /usr/local/bin/nvim
          nvim --version

      - name: Run tests
        run: |
          nvim --headless \
            -u tests/minimal_init.lua \
            -c "PlenaryBustedDirectory tests/ {minimal_init = 'tests/minimal_init.lua'}" \
            -c "qa!"

  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install stylua
        run: |
          curl -sL -o stylua.tar.gz https://github.com/JohnnyMorganz/stylua/releases/latest/download/stylua-linux-x86_64.zip
          unzip stylua.tar.gz -d /usr/local/bin
          chmod +x /usr/local/bin/stylua
          stylua --version

      - name: Check formatting
        run: stylua --check lua/ tests/

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Neovim
        run: |
          curl -sL -o nvim.tar.gz https://github.com/neovim/neovim/releases/latest/download/nvim-linux-x86_64.tar.gz
          sudo rm -rf /opt/nvim
          sudo tar -C /opt -xzf nvim.tar.gz
          sudo ln -sf /opt/nvim-linux-x86_64/bin/nvim /usr/local/bin/nvim
          nvim --version

      - name: Check Lua syntax
        run: |
          shopt -s globstar
          for f in lua/**/*.lua tests/**/*.lua; do
            echo "Checking $f"
            nvim --headless -c "luafile $f" -c "qa!" 2>/dev/null || exit 1
          done
          echo "All files OK"
        shell: bash
